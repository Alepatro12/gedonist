/**
 * @const
 * @type {number} User type - default
 */
export const USER_TYPE_DEFAULT = 1;

/**
 * @const
 * @type {number} User type - administrator
 */
export const USER_TYPE_ADMINISTRATOR = 2;

/**
 * @const
 * @type {number} User type - moderator
 */
export const USER_TYPE_MODERATOR = 3;
